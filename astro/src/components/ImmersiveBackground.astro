---
---

<div class="immersive-bg" id="immersive-bg">
    <video class="immersive-video" id="bgVideo" autoplay muted loop playsinline>
        <source id="bgSource" src="/bg1.mp4" type="video/mp4">
    </video>
    <div class="vignette-overlay"></div>
</div>

<style>
    .immersive-bg {
        position: fixed;
        inset: 0;
        z-index: -1;
        overflow: hidden;
        background: linear-gradient(135deg, #08060e 0%, #1a0f2e 50%, #0d0620 100%);
        pointer-events: none;
    }

    .immersive-video {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        opacity: 0.5;
        z-index: -1;
        image-rendering: high-quality;
        filter: contrast(1.1) saturate(1.15) brightness(1.05);
    }

    .vignette-overlay {
        position: absolute;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        background: radial-gradient(ellipse at center, transparent 50%, rgba(8,6,14,0.3) 100%);
    }

    /* 全域深色背景 */
    body {
        background: rgba(8, 6, 14, 0.95) !important;
    }

    main, header, section, article, footer, aside, nav {
        background: rgba(8, 6, 14, 0.95) !important;
    }

    /* 每个文字元素有方块背景（内联显示） */
    p, h1, h2, h3, h4, h5, h6, span, a, li, td, th, label,
    .hero, .container, .panel, .card, .surface-card, .runtime-card, .metric-card,
    .demo-card, .skill-card, .timeline-card, .link-card {
        display: inline;
        background: rgba(8, 6, 14, 0.95) !important;
        padding: 2px 4px;
        margin: 1px;
        border-radius: 3px;
    }

    /* 块级元素特殊处理 */
    p, h1, h2, h3, h4, h5, h6, li, td, th, article, section, main, header, footer {
        display: block;
        background: rgba(8, 6, 14, 0.95) !important;
        padding: 4px 8px;
        margin: 2px 0;
        border-radius: 4px;
    }

    /* 确保链接和span等行内元素有背景 */
    span, a, strong, em, b, i {
        background: rgba(8, 6, 14, 0.95) !important;
        padding: 2px 4px;
        border-radius: 3px;
    }

    .particle {
        position: absolute;
        width: 2px;
        height: 2px;
        background: radial-gradient(circle, rgba(255,255,255,0.8), rgba(255,255,255,0));
        border-radius: 50%;
        pointer-events: none;
        animation: float 20s infinite ease-in-out;
    }

    @keyframes float {
        0%, 100% { 
            transform: translateY(0) translateX(0) scale(1); 
            opacity: 0; 
        }
        10% { opacity: 1; }
        50% { opacity: 0.8; }
        90% { opacity: 0; }
    }
</style>

<script>
    const videos = [
        '/bg1.mp4', '/bg2.mp4', '/bg3.mp4', '/bg4.mp4', '/bg5.mp4',
        '/bg6.mp4', '/bg7.mp4', '/bg8.mp4', '/bg9.mp4', '/bg10.mp4',
        '/bg11.mp4', '/bg12.mp4', '/bg13.mp4', '/bg14.mp4', '/bg15.mp4'
    ];
    let currentIndex = Math.floor(Math.random() * videos.length);
    let lastIndex = currentIndex;

    function getRandomVideo() {
        while (currentIndex === lastIndex && videos.length > 1) {
            currentIndex = Math.floor(Math.random() * videos.length);
        }
        lastIndex = currentIndex;
        return videos[currentIndex];
    }

    function switchVideo() {
        const video = document.getElementById('bgVideo');
        const source = document.getElementById('bgSource');
        if (!video || !source) return;
        const newVideo = getRandomVideo();
        source.src = newVideo;
        video.load();
        console.log('Switched to:', newVideo);
    }

    class ImmersiveBackground {
        constructor() {
            this.container = document.getElementById('immersive-bg');
            this.particles = [];
            this.init();
        }

        init() {
            this.createParticles();
            this.setupEventListeners();
            setInterval(() => {
                const video = document.getElementById('bgVideo');
                if (video && !video.paused) {
                    switchVideo();
                }
            }, 15000);
        }

        createParticles() {
            if (!this.container) return;
            const particleCount = Math.min(80, Math.floor(window.innerWidth / 15));
            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                const size = Math.random() * 3 + 1;
                particle.style.width = size + 'px';
                particle.style.height = size + 'px';
                particle.style.animationDuration = (Math.random() * 10 + 15) + 's';
                particle.style.animationDelay = Math.random() * 5 + 's';
                this.container.appendChild(particle);
                this.particles.push(particle);
            }
        }

        setupEventListeners() {
            window.addEventListener('resize', () => this.handleResize());
        }

        handleResize() {
            const newParticleCount = Math.min(80, Math.floor(window.innerWidth / 15));
            if (newParticleCount !== this.particles.length) {
                this.particles.forEach(p => p.remove());
                this.particles = [];
                this.createParticles();
            }
        }
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', () => {
            new ImmersiveBackground();
        });
    } else {
        new ImmersiveBackground();
    }
</script>