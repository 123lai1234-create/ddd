{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "cd astro && npm install && npm run build",
    "vercel-build": "cd astro && npm run build"
  },
  "dependencies": {
    "@vercel/og": "^0.6.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1"
  }
}
